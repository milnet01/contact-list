# Lane 7 — frontend-js (static/app.js, 581 lines / 23,875 bytes)
## Critical (0)
## High (3)
- [dim 2b] app.js:346-348 — `if (!container || !addBtn) { return; }`. #custom-fields/#add-field exist in EXACTLY ONE template, contact_form.html. Everything below :348 runs only on /contacts/new and /contacts/<id>/edit. Stranded exhaustively: (a) setHeaderHeight() + its resize listener (:513-521), so --header-h is never published on any other page — and style.css:344-346 .list-controls uses it on /contacts ONLY, the one page that can never set it; the measurement feature is 100% dead and the sticky bar always uses the hardcoded 56px fallback. (b) #back-to-top (:523-533), which base.html:54 ships on EVERY page. NOTHING ELSE is stranded — the tail was enumerated. fix: hoist :513-533 into their own IIFE; do not reorder the return.
  ORCHESTRATOR VERIFICATION: CONFIRMED precisely — the early return is inside the first IIFE (lines 2-534); the stranded blocks are at 508-533, after it. #custom-fields appears only in contact_form.html; #back-to-top only in base.html; --header-h is consumed at style.css:346. Already tracked as CL-0048.
- [dim 2] app.js:198-204 + duplicates.html:58,98,138 — the delete confirmation states the WRONG number on /contacts/duplicates. find_all_duplicates returns three independent bucketings (name, email, phone), so a contact duplicated by both name and email renders two .row-select checkboxes with the same value. "Delete 5 selected contacts?" can precede a delete of 3. The same double-count feeds bulkCount and puts repeated values into the bulk-delete and merge POSTs. fix: count distinct values with a Set.
- [dim 7] app.js:216-239 — the try guards the PARSE, not the SHAPE. JSON.parse of a tampered value yields any JSON type; recent.filter throws TypeError on an object, r.name.charAt(0) throws on an array of strings. An uncaught throw here aborts the rest of the IIFE — before layoutCardMasonry, before custom fields, before header-height and back-to-top. One bad localStorage key silently kills card view AND every enhancement below it, on every page, permanently.
## Medium (5)
- [dim 3] app.js:258 — link.setAttribute('href', r.href) is the only unsanitised sink; r.href is read back from localStorage, never re-validated, so a stored javascript: value becomes a live link. Writing it needs same-origin access, but the sink is unguarded.
- [dim 2] file size vs DESIGN 14 — the budget table says JavaScript < 10 KB; the file is 23,875 bytes, 2.4x over. 14 calls only the Python row a soft target, so the JS row reads as a hard budget. Either side could be wrong.
- [dim 12] app.js:96-123 — three single-character shortcuts with no modifier guard, no disable, no focus scoping. Ctrl+S/Cmd+S is swallowed by preventDefault; `n` navigates away while the confirm modal is open. WCAG 2.2 2.1.4 (Level A), reported on general principles.
- [dim 12] app.js:191-195 — #bulk-form contains only checkboxes so it has no implicit-submission surface to protect; the handler guards nothing and cancels Enter activation on Merge/Delete/Clear.
- [dim 5] app.js:317-325 — getBoundingClientRect read inside the same loop that writes style.left/top: read-after-write thrashing, one forced reflow per card, up to ~200 per resize tick.
## Low / Info
- [dim 4] app.js:255-256 vs contacts.html:110-111 — two diverged copies of the avatar-class derivation. Python's isalpha() is true for É so the template emits avatar-É, a class that does not exist; the JS copy correctly falls back to avatar-other.
- [dim 2] app.js:531 — smooth scroll ignores prefers-reduced-motion, which DESIGN 10 promises; style.css:1079-1085 covers animations and transitions but not programmatic scrolling.
- [dim 2] DOC SIDE — DESIGN 5/5.1.3/10 describe app.js as doing search/filter/live search. There is NO client-side search or filter code anywhere; search is a plain server-side GET form. Hand to review-contract.
- [dim 12] app.js:14-18 flashes removed after a fixed 6s with no pause (WCAG 2.2.1); :146-155 selection count changes with no aria-live (4.1.3); showModal focuses #confirm-yes but nothing restores focus on cancel and there is no focus trap.
- [dim 16] app.js:69-90 — confirming is set before submit and never cleared on that branch, so if navigation does not complete the button stays armed and the next click submits with NO confirmation. Also preventDefault runs before the `if (!msg) return;`, so data-confirm="" makes a control silently inert.
- [dim 16] app.js:181-187 — bulkClear resets .row-select but not the .group-select header boxes; #select-all is never set indeterminate.
- [dim 5] app.js:290-291 — form.querySelector('tbody') takes only the FIRST tbody; duplicates.html has one per group, so only the first group gets masonry.
- [tool: eslint] :233/:243 var recent declared twice in one scope (no-redeclare); :229 var contactId leaks; :541 the second IIFE has no 'use strict' where the first does; :526 window.pageYOffset is the deprecated alias; :39 modalYes.focus() unguarded while the same function guards modal/modalMsg. None ships a bug today; all flagged because NO JavaScript linter has ever run on this file.
- INFO — CSRF: there are ZERO fetch/XMLHttpRequest/sendBeacon call sites in the file, and base.html:68 is the only <script> in any template. Every state change is a native form POST and a _csrf_token hidden input was confirmed in each form this script actuates. Nothing to report; there was nothing to check.
- INFO — DOM XSS: zero innerHTML/outerHTML/insertAdjacentHTML/document.write/eval/new Function. The recently-viewed builder uses createElement/textContent/createTextNode and replaceChildren. The only attacker-influenceable string reaching a DOM API is the href filed above.
## Covered by spec and looks correct
All four DESIGN 10.1 tabs clauses: .tabs hidden by CSS and revealed only by js-tabs so JS-off shows all panels; tab buttons type="button"; confirms use data-confirm with no inline JS; roving tabIndex + aria-selected + Home/End/Arrow all correct. CSP: every style write is a CSSOM property assignment, not setAttribute('style') or cssText. Masonry height stability: fixed avatar box and no @font-face, so lazy photos and font swaps cannot shift measured heights. Drag-and-drop reorder has a keyboard equivalent with aria-labels.
## Open questions
1. Is DESIGN 14's JS budget a hard gate or, like the Python row, guidance?
2. Do repeated `selected` values from duplicates cause a double delete server-side? The finding stands on the displayed count regardless.
3. Is bulk merge intended to work with JS off? #bulk-bar is unhidden only by updateBulkBar.
## 3 to fix first
1. Hoist :513-533 out of the custom-fields IIFE. 2. Validate the SHAPE of the recently-viewed list. 3. Count distinct ids before the delete confirmation.
