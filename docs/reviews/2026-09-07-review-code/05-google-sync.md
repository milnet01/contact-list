# Lane 5 — google-sync (google_sync.py 618, google_auth.py 53, photos.py 208) + spec 2026-07-02-two-way-google-sync.md
## Critical (0)
## High (5)
- [dim 9] google_sync.py:208 — the expired-sync-token self-heal does DELETE FROM sync_state WHERE id = 1, wiping last_synced_at as well as sync_token. Spec 5 Step 3 decouples them precisely so last_synced_at never goes stale. If the restarted full pull then fails on a later page, Step 3 never runs and last_synced_at stays NULL. On the NEXT sync prev_sync is NULL -> get_dirty_linked returns zero rows -> dirty_google_ids empty -> the pull's deferral at :525 is disabled -> _upsert_person overwrites every locally-edited contact with Google's copy, and those edits were never pushed. Silent user-data loss. fix: UPDATE sync_state SET sync_token = NULL WHERE id = 1.
  ORCHESTRATOR VERIFICATION: CONFIRMED — sync_state has exactly three columns (id, sync_token, last_synced_at) per migrations/001_initial.sql:29-33, so the DELETE takes last_synced_at with it.
- [dim 2] google_sync.py:374 — body['birthdays'] = [{'date': date}] is the ONE managed field that does not go through _merge_primary, against spec 6 and INV-2 ("a push never removes a value the app does not manage"). A contact with a second birthday entry, or a text field on entry 0, loses it on every push.
  ORCHESTRATOR VERIFICATION: CONFIRMED — names, emailAddresses, phoneNumbers, biographies, addresses and organizations all call _merge_primary; birthdays alone assigns directly.
- [dim 2] google_sync.py:354-369 — a field the user CLEARS locally is omitted from both body and fields, so updatePersonFields never names it and Google keeps the old value; the next pull re-imports it, silently reverting the deletion. Spec 7 promises "local wins"; a clearing edit can never win. Same for birthday/address/organization at :372-385.
- [dim 2] _push_local_changes (:422-474) — spec 9 promises a 403-on-write branch surfacing "Google needs the write permission — reconnect." No such branch exists; the string appears nowhere in the tree. A 403 falls into the generic except at :471 and becomes result.skipped += 1. A documented feature with no code behind it.
- [dim 3] google_sync.py:114-117 — revoke_credentials deletes the local token and never contacts Google. DESIGN 6.2 says "Revoke and re-auth"; 9 documents POST /sync/disconnect as "Revoke Google credentials"; the UI flashes "Disconnected from Google." The refresh token stays live at Google, so any copy of token.json still grants full read-write contacts access after "disconnecting".
  ORCHESTRATOR VERIFICATION: CONFIRMED — no .revoke( and no oauth2.googleapis.com/revoke anywhere in the tree. Corroborated independently by lane routes-io.
## Medium (9)
- [dim 3] :483 — the host allow-list validates only the INITIAL url. urlopen's default opener includes HTTPRedirectHandler, which permits redirects to http/https/ftp, so a 3xx from *.googleusercontent.com to http://127.0.0.1:5002/ is followed. The SSRF guard does not survive a redirect. (Verified sound in the same guard: .hostname strips userinfo and lowercases; trailing-dot FQDN and non-ASCII confusables fail closed.)
- [dim 16] :429-474 — no rate-limit handling anywhere; no num_retries, no 429/RESOURCE_EXHAUSTED/5xx branch. Spec 2 mandates a one-time bulk create of every local-only contact; on a few thousand contacts the write quota trips and all remaining land in skipped, and the next sync retries from scratch.
- [dim 8] :616 inside the SAVEPOINT at :233-238 — a photo download (10s socket timeout, no total budget) runs while the per-contact write transaction is open, holding the WAL write lock. db.py:38 sets busy_timeout=5000, so a concurrent UI write fails SQLITE_BUSY after 5s.
- [dim 5] :616,:487-512 — the photo is re-downloaded on EVERY upsert of every returned person; nothing stores the photo URL or etag. On a full first sync that is one round trip of up to 4 MiB per contact against DESIGN 7.1's "1000 contacts < 10 s".
- [dim 9] photos.py:163-171 — delete_photo runs BEFORE the new open(...,'wb'). If the write fails the existing photo is gone while contact_photos.ext still names it, so the avatar 404s permanently. The original is written non-atomically, unlike the thumbnail at :138-140 which correctly does tmp+os.replace.
- [dim 9] photos.py:136 — tmp name is unique per PROCESS, not per writer. Two concurrent GETs for the same avatar on the server thread share one tmp path; B truncates while A is between write and replace, so a half-written file is promoted and then persists forever.
- [dim 2] :525 vs :529 — the deferral check runs BEFORE the deleted-tombstone check, so a tombstone for a locally-edited contact is skipped and consumed by this run's syncToken, never redelivered. The local row survives as a linked orphan pointing at a deleted resourceName and is never dirty again.
- [dim 2] :318-329 used at :355 — _merge_primary sets unstructuredName while re-sending Google's givenName/familyName verbatim; per People API semantics unstructuredName is parsed only when structured parts are absent, so a rename may not take effect. Same for addresses' formattedValue beside untouched components.
- [dim 16] :91-96 — a transient network failure is indistinguishable from an invalid grant; both make is_authenticated False while needs_reconsent is False, so /sync invites a full re-auth for a temporary outage. DESIGN 6.2 asks for revoke-and-re-auth on PERSISTENT RefreshError; the code distinguishes neither.
## Low / Info
- [dim 2] :491-492 — cites INV-5 and INV-6 for the photo guard; INV-5 is the OAuth scope invariant, INV-6 is push-create idempotency, and the spec has NO photo invariant.
- [dim 3] :108-111 and google_auth.py:43-46 — os.open(0o600) then write then chmod; a pre-existing 0644 token.json is world-readable for the duration of the write. fix: os.fchmod immediately after os.open.
- [dim 7] :236-239 — if the exception originates in the SAVEPOINT statement itself, ROLLBACK TO SAVEPOINT raises "no such savepoint" inside the except, uncaught, aborting the sync and discarding result.pulled.
- [dim 7] :509 — no exc_info, and photo failures are counted in no SyncResult field, so a systematic failure is invisible.
- [dim 7] :407-411 — _push_update returns False on empty fields; the caller increments neither updated nor skipped.
- [dim 2] :197-207 — the comment's "can only run before any page has been committed" rests on synced == 0, but synced counts IMPORTS; a first page of pure tombstones commits real DELETEs and still leaves synced == 0. Benign, but the stated guarantee is not the one the guard provides.
- [dim 9] :531 — DELETE cascades DB rows but leaves photo files on disk forever. AUTOINCREMENT means no orphan is served for a different contact, but there is no eviction.
- [dim 2][tool: mypy] untyped params throughout both modules; pyproject sets neither disallow_untyped_defs nor strict.
- [dim 13] :459 — compares Google's server clock to a last_synced_at written by SQLite's 'now' (local clock). A local clock running ahead makes google_changed permanently False. Spec 7 sanctions this as immaterial, reasoning about human edit spacing rather than NTP jumps — SPEC is the side to revisit.
- [dim 5] :499-500 — urlparse called twice on the same string.
- [dim 2] spec 8.1/12 promise a per-contact skipped/conflict list by NAME; SyncResult carries only integers. Lean code.
- INFO — could not run mypy, so the brief's claim that photos.py "has never been type-checked" is flagged unverified: pyproject's files list omits it, but default follow_imports=normal means it is probably analysed transitively via routes.
## Covered by spec and looks correct
INV-4: no deleteContact anywhere. INV-5: SCOPES a single literal, _token_has_write_scope omits the scopes argument exactly as spec 3 requires. _parse_dt normalises trailing Z, defaults naive to UTC, and on 3.12+ truncates >6 fractional digits. The LWW branch matches spec 7 including tie-goes-to-Google and push_no_time. MAX_PHOTO_BYTES+1 is sound — no zip-bomb path. PKCE present via google_auth_oauthlib (code_challenge_method=S256), run_local_server binds localhost. Per-record SAVEPOINT isolation HOLDS on 3.12+: SAVEPOINT person is outermost so it opens the transaction, and RELEASE on an outermost savepoint commits. The only leak out of that isolation is the filesystem.
## Open questions
1. Is field-clearing intentionally "set, never clear"? Neither spec nor DESIGN 8.2 says.
2. Does updateContact honour a changed unstructuredName when givenName/familyName are also present? Not answerable from this repo.
3. Was the per-contact skipped/conflict name list dropped deliberately or is it unfinished?
## 3 to fix first
1. :208 DELETE FROM sync_state — only finding whose failure mode is irreversible loss of the user's own edits, on a routine self-healing path, one-line fix.
2. :374 birthdays — a quotable INV-2 violation in the one field that escaped the helper written to prevent it.
3. :483 SSRF redirect hole — the only network sink in the app; the guard is the documented mitigation and does not survive a 3xx.
