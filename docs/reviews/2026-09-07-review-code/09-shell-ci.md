# Lane 9 — shell-ci (run.sh, local-ci.sh, packaging/*, contact-list.spec, workflows, pre-push)
## MIRROR CLAIM VERIFIED LINE BY LINE
matrix ci.yml:18 ["3.12","3.13","3.14"] == local-ci.sh:34 — MATCH
dev-tool pins ci.yml:36 'ruff~=0.16.1' 'mypy~=2.1' == local-ci.sh:38 DEV_TOOLS — MATCH
three checks same order ci.yml:39/42/45 == local-ci.sh:108/109/110 — MATCH
unobtainable Python is a FAILURE — local-ci.sh:117-121 exits 1 — MATCH
docs-only skip — pre-push:48-55 regex (\.md$|^docs/) — MATCH
SKIP_LOCAL_CI=1 — pre-push:19-22 — MATCH
The three named axes hold. Two UNNAMED divergences break the stronger sentence "a local pass really does mean all jobs" — H1 and M8. Gitignored trees are excluded by ruff respect-gitignore and mypy files/exclude, so NOT divergences.
## Critical (0)
## High (4)
- [dim 2] local-ci.sh:104 — the cached venv NEVER moves within a version range. requirements.txt pins majors only; CI builds fresh and resolves newest-in-range, while `pip install -r` without --upgrade reports "already satisfied". The :95 stamp (sys.version | DEV_TOOLS | sha256 requirements.txt) does not change when UPSTREAM publishes flask 3.2.0 or ruff 0.16.5, so the rebuild at :96-99 never fires. A green local run can be produced on a dependency set CI will never install — the exact stale-env false pass the :88-93 comment claims to have closed — and it silently defeats DESIGN 3 "dependencies track latest" locally. fix: --upgrade on both installs, or a date component in the stamp.
- [dim 2b] packaging/check-version-drift.sh — config.py:8-9 states this script "enforces the lockstep". NOTHING COMMITTED INVOKES IT: not ci.yml, not release.yml, not pre-push, not local-ci.sh. Its only caller is .claude/bump.json, and .gitignore:18 is .claude/ — so the file is absent from every clone. release.yml publishes binaries on any v* tag with no version check at all (v2.0.0 over APP_VERSION 1.1.0 ships happily).
  ORCHESTRATOR VERIFICATION: CONFIRMED — grep finds no caller anywhere in the tree; .claude/ is gitignored and git ls-files .claude/ is empty.
- [dim 2] build-linux.sh:29-30 vs contact-list.spec:30-33 — the probe tries classic AppIndicator3 FIRST and falls back to Ayatana, but the spec's hiddenimports list only AyatanaAppIndicator3. On a host carrying the classic typelib and not Ayatana, the probe passes on branch one, PyInstaller cannot resolve the Ayatana hidden import, logs a warning and EXITS 0 with a tray-less AppImage — the precise CL-0057 failure the pre-flight was added to stop. The tray spec records that the target user's own machine is exactly that host.
  ORCHESTRATOR VERIFICATION: CONFIRMED — build-linux.sh:29 tries AppIndicator3, :30 falls back to Ayatana; contact-list.spec:32 lists only gi.repository.AyatanaAppIndicator3.
- [dim 7] .githooks/pre-push:40 — `changed="$changed$(git diff --name-only "$base" "$local_sha" 2>/dev/null)"` discards a git diff FAILURE, leaving changed as whitespace only; :44's empty test then EXITS 0 AND SKIPS CI ENTIRELY. Reachable when remote_sha names a commit the local repo does not have (remote ahead, force-push, pruned objects). Indistinguishable from "nothing to push". The RUN_ANYWAY fallback at :36-39 covers only an empty base.
  ORCHESTRATOR VERIFICATION: CONFIRMED by reading — the 2>/dev/null and the subsequent whitespace-only exit 0 are both present as described.
## Medium (7)
- [dim 7] local-ci.sh:101-105 — venv creation, the pip upgrade and both pip installs run outside run_step and outside set -e (:28 is set -uo pipefail). None increments failures. Concrete false pass: stamp matches, `pip install -r` fails on an index outage, cached tools still run, and :122 prints "CI PASSED: all checks green across the full Python matrix".
- [dim 2] run.sh:35 — the per-launch sync omits the --ignore-installed that :29 uses, and :26-28 explains why the flag is needed under --system-site-packages. A dependency newly added to requirements.txt and already present system-wide inside our range is borrowed from the distro, so the comment's promise does not hold for exactly the packages the distro also ships.
- [dim 16] run.sh:3 + :35 — set -euo pipefail makes the unconditional per-launch pip install FATAL. With PyPI unreachable, an app that worked yesterday will not start at all, with a pip traceback and no fallback.
- [dim 7] check-version-drift.sh:9,13-15 — under set -euo pipefail a plain assignment takes the command substitution's exit status, so a failed grep terminates the script BEFORE the "could not extract APP_VERSION" message can run. Lines 13-15 are unreachable; same for log_ver at :11. Answering the brief: it cannot pass on a malformed version — it fails closed — but with exit 1 and no diagnostic whatsoever.
- [dim 4] make-icons.sh:6-11 — the interpreter-search prologue is duplicated verbatim from build-linux.sh:5-10, and only the latter was given the empty-$PY guard (build-linux.sh:21-24). The tray spec states why that guard exists. make-icons.sh:13 is the unguarded case and build-macos.sh:9 is a third variant. One copy fixed, two not.
- [dim 3] wine-setup.sh:21-22,27 — a downloaded 25 MB EXECUTABLE is run with no integrity check, and a cached copy is re-executed unverified on every run. build-linux.sh:56-62 establishes the project's own standard (fetch_verify, sha256 re-checked every run, precisely so a truncated cached file is caught not reused) and this script does not follow it.
- [dim 2] release.yml:6-9 vs :77 — the comment asserts workflow_dispatch NEVER publishes a Release. The condition is startsWith(github.ref,'refs/tags/'), which does not test for `v` and IS satisfied by a workflow_dispatch run launched against a tag ref. A manual rehearsal on tag v1.1.0 re-publishes that release.
## Low / Info
- [dim 2] pre-push:61 runs local-ci.sh against the WORKING TREE while the docs-only decision at :40 comes from the commits being pushed. Dirty state is what gets tested, in both directions.
- [dim 16] build-linux.sh:58-62 — a partial cached download is caught but never recovered; the script never deletes the bad file, so the build stays permanently wedged behind a bare sha256sum WARNING.
- [dim 7] local-ci.sh:58 — `uv python install "$ver" >&2 2>/dev/null` sends uv's stdout to the terminal and its STDERR to /dev/null, discarding the one message explaining why a matrix Python could not be fetched.
- [dim 11] pre-push:17 — Z40 is SHA-1 only; a sha256 object-format repo sends 64 zeros and both the branch-deletion and new-branch branches misfire.
- [dim 11] release.yml:37 — build-linux has no setup-python, so the shipped AppImage's Python is whatever ubuntu-latest ships and drifts with the image, while Windows and macOS are pinned to 3.12. Likely forced (system python3-gi must match the system interpreter) — worth stating in the file.
- [dim 11] pyproject.toml:10,26 — target-version py312 and python_version 3.12 mean the 3.13 and 3.14 legs run byte-identical ruff and mypy analyses; only pytest genuinely varies across the matrix.
- [dim 7] release.yml — nothing makes the release path depend on ci.yml, which triggers on branches:[main] only, so a v* tag push never runs ruff/mypy/pytest. Binaries can be published from a commit whose CI was red.
- [dim 2][tool: ruff] contact-list.spec:14 — SPECPATH is noqa'd but Analysis, PYZ, EXE, COLLECT and BUNDLE are equally injected and equally undefined; with F selected all six would be F821 if the file were ever linted. ruff does not recognise .spec and mypy's files omits it, so it has had no mechanical check at all.
- [dim 2][tool: ruff] contact-list.spec:4,14-21 — os.path throughout against python.md's pathlib idiom. Latent; PTH not selected.
- [dim 16] pre-push — nothing verifies core.hooksPath is set, so a fresh clone has no gate and no warning.
- N/A: 5, 8, 9, 10, 12, 13, 15, 17 — each with a stated reason.
## Covered by spec and looks correct
build-linux.sh:37-66 — appimagetool and runtime downloads pinned by URL AND sha256, re-verified every run before chmod +x, with the rolling `continuous` tag's risk stated in-file. run.sh:14-20 — the venv rebuild guard checks both the include-system-site-packages marker and that the base interpreter still runs, matching the two failure modes CL-0057 records. release.yml:11,78-79 — top-level contents: read with contents: write scoped to the release job only is correct least privilege; no secrets referenced anywhere. download-artifact@v8 with no name does place each artifact in a directory matching files:. Every rm -rf targets a variable set -u guarantees non-empty. Artifact names produced by contact-list.spec match every consumer path.
## Open questions
1. release.yml:53 runs make-icons.sh on windows-latest; its search loop prefers python3, which on Windows can resolve to the Store app-execution-alias stub. Could not determine whether setup-python shadows it.
2. Is .claude/bump.json intended to stay gitignored? If yes, the H2 fix must be a CI step rather than re-tracking the recipe.
3. contact-list.spec:47 pathex=[] is the exact remedy CL-0061 proposes. Left as a known open item — but until it lands build-linux.sh has NO way to tell a spurious "Hidden import not found" from a real one, and nothing greps PyInstaller's output, so a genuinely incomplete bundle exits 0.
## 3 to fix first
1. pre-push:40 — a gate that silently passes is worse than no gate. 2. local-ci.sh:104 --upgrade. 3. build-linux.sh/spec AppIndicator3 hidden import.
