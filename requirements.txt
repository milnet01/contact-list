# Requires Python 3.12+ — the Google-sync per-record SAVEPOINT isolation relies
# on 3.12+ sqlite3 transaction semantics (see DESIGN.md §3, CL-0019).

# --- Runtime dependencies ---
flask>=3.1.1,<4.0
google-api-python-client>=2.0,<3.0
google-auth>=2.0,<3.0
google-auth-oauthlib>=1.0,<2.0
google-auth-httplib2>=0.2,<1.0
phonenumbers>=9.0,<10.0
pillow>=12.0,<13.0
pystray>=0.19,<0.20

# --- Test dependencies (not needed to run the app) ---
pytest>=9.0,<10.0
